const activeIsers: [] = []; //this is an empty array

const activeUsers: string[] = [];
activeUsers.push('Deniz');

const bools: Array<boolean> = []
//const bools: boolean[] = []

type Points = {
    x: number,
    y: number,
}

const coords: Points[] = [];
coords.push({x:23, y:34});

//multidimensional arrays

const board: string[][] = [
    ['x', 'O', 'X'], 
    ['x', 'O', 'X'], 
    ['x', 'O', 'X']
];

const demo: string[][][] = [[['a']]]